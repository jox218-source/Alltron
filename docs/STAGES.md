# Alltron stages and publication gates

Evidence date: 2026-09-26. Stages 0–1 and the first Stage 2 prototype are merged. A read-only review of a separate private full-release preview supplied locally checked *design patterns* for archive integrity, staged installation/rollback, owner-selected Home Assistant entities and voice asset checks. None of its code, household data, assets or hardware results are Alltron acceptance evidence. The existing private Pi and Home Assistant installation remain outside this project. Stage 3 software work runs alongside unfinished Stage 2 integrations; dedicated-Pi work remains last. [PR #9](https://github.com/jox218-source/Alltron/pull/9) now carries the source archive and local preview install/recovery slices from merged feature-branch PRs #10 and #11; integration to `main` remains pending.

| Stage | Work and exit evidence | Current state |
| --- | --- | --- |
| **0. Public boundary and reuse map** | Keep the separate repo, no-reply Git identity, approved paths, privacy audit, license/security process and fictional fixtures. Convert private-app observations into a feature/contract checklist without importing its code, data or assets. | Publication gate and [sanitized reuse checklist](REUSE_CHECKLIST.md) prepared; repeat exact-SHA review before every push. Root and PR #1 merge email exceptions documented. |
| **1. Local software foundation** | Deterministic commands, durable timers/alarms/lists, cancellable voice event contracts, separate wake/STT/TTS status and opt-in service-owned push-to-talk adapters. Verify routing, persistence, retries, cancellation and HTTP behavior with invented fixtures and fake audio. | Software foundation merged in [PR #4](https://github.com/jox218-source/Alltron/pull/4); simulated tests pass. The speech adapters have no live audio acceptance. |
| **2. Owner connections and isolation — finish in software** | On disposable Linux, install a throwaway same-device HA Container, complete owner authorization/revocation and explicit entity selection, and verify bounded on/off actions and outages. In parallel, run Codex CLI under a genuinely separate OS/container identity with an owner-authorized sign-in in a disposable environment; test answer-only routing, auth/usage/network failures, timeout and cleanup. Keep the provider disabled in the running service until isolation passes. | The merged [Stage 2 prototype PR #7](https://github.com/jox218-source/Alltron/pull/7) has a token-file HA allowlist, fake HA tests and a fake-tested Codex transport. Real HA setup, owner UX and isolated signed-in Codex remain open. See [Stage 2 development](STAGE2_DEVELOPMENT.md). |
| **3. Portable software release — alongside Stage 2** | Re-author the private preview's useful *contracts* in Alltron: explicit archive inventory and per-file hashes, separate secrets/models, preflight, staged app/HA install, service and kiosk setup, failed-update rollback, backup and uninstall. Add a guided setup flow, redacted diagnostics and simulated audio checks. Rehearse clean and repeat Linux/HA installs, outage/recovery and an independent software-only owner trial. | Open [PR #9](https://github.com/jox218-source/Alltron/pull/9) includes a deterministic source archive, unprivileged local preview preflight/install/code rollback and application database recovery with simulated tests. It is not an appliance installer. HA/service installation, owner wizard, migrations and fresh-owner acceptance remain. Cover time-zone behavior with fictional Alltron fixtures and IANA data in disposable Linux; the private preview's Windows failure did not establish an Alltron defect. |
| **4. Separate-Pi acceptance — last technical gate** | On a separately identified and allowlisted Pi, review exact Whisper/Piper/wake asset licenses and provenance, finish wake/resident speech, then verify microphone, speaker, normal-volume wake, recognition/reply, silence/near-match/background/long speech, alarm sound, ARM64 dependencies and full-stack RAM/latency/thermal behavior. Rehearse reboot, network loss, HA/Codex auth expiry, update/rollback and an independent fresh-owner install. | Waiting for dedicated hardware. The private second-Pi package has no Alltron or fresh-device acceptance and cannot substitute for this gate. |
| **5. Public beta — release gate** | Publish only the tested hardware/OS profile, actual limitations, exact installer and recovery steps, audited source/archive and third-party license/asset notices. Advertise optional signed-in answers only if Stage 2 isolation and owner setup passed. | Waiting for Stages 2–4; no beta claim. |

**Critical path:** close the two Stage 2 integration gates while extending Stage 3's local preview manager into a disposable Linux service/HA installer and guided setup. Join them in a clean disposable Linux install and independent software setup trial; only then move to Stage 4 physical acceptance and Stage 5 beta. A private preview, synthetic test, checksum or desktop run is not proof of a public appliance. No purchase or use of the current household Pi is needed for Stages 2–3.

## Agent assignments and review

GPT-6 Luna Medium can implement fictional fixtures, UI, docs, CI, tests and narrow public modules under agreed contracts. Stage 2 HA/Codex and Stage 4 voice can have separate implementation agents only in an isolated Alltron-only OS/container environment without private mounts or credentials. Same-host worktrees do not provide that isolation; on the current host, Luna is restricted to non-sensitive public tasks. GPT-6 Sol High reviews the reuse map and contracts, security-sensitive boundaries, integrated diffs, dependency provenance and exact public push range. The lead integrates and records release evidence.

## Privacy gate before every GitHub push

1. Start from the public Alltron history. Never import the private project's history, source tree, symlink, state, recordings, photos, models, credentials or deployment targets. Review an explicit path allowlist.
2. Run [the publication gate](PUBLICATION_GATE.md). Inspect **all new commits and Git objects**, including deleted files, binary assets and author/committer metadata. Run a secret scanner and manually check for names, addresses, device IDs, calendar data, media and personal configuration; `.gitignore` alone is insufficient.
3. If any questionable content entered a commit, rebuild from the clean public base before pushing. Removing it in a later commit does not remove exposure from public history.
4. Sol High reviews the exact SHA and final diff before a branch push. Use GitHub protection and scanning as additional controls, and verify release archives separately.
5. Keep every fixture invented and every support bundle redacted. Never publish account tokens, Codex auth state, real household data or current-Pi addresses. Source/model licensing and provenance review remains required even when using the same upstream engines as the private app.

The initial public GitHub commit and the already-merged PR #1 squash commit contain a personal email in Git author metadata. The development commits use the chosen GitHub no-reply identity. Both published exposures are documented in [PUBLICATION_GATE.md](PUBLICATION_GATE.md); a later commit cannot make public history private.
