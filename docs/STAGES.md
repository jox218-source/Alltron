# Alltron stages and publication gates

Evidence date: 2026-09-24. This compressed software-first plan uses the Alltron timer preview and design lessons from the private app's 2026-09-23 local speech pilot. Only the timer preview and Stage 0 safety gate are implemented in Alltron. The private Pi and Home Assistant installation are outside every stage. Physical testing uses a separate device after software and simulated acceptance are as complete as possible.

| Stage | Work and exit evidence | Current state |
| --- | --- | --- |
| **0. Public boundary and reuse map** | Keep the separate repo, no-reply Git identity, approved paths, privacy audit, license/security process and fictional fixtures. Convert private-app observations into a feature/contract checklist without importing its code, data or assets. | Publication gate and [sanitized reuse checklist](REUSE_CHECKLIST.md) prepared; repeat exact-SHA review before every push. Legacy root-email exception documented. |
| **1. Local appliance core and speech** | Extend the existing Alltron timer preview with deterministic commands, durable alarms/lists, cancellable voice events and separate wake/STT/TTS health. Implement a clean speech worker, service-owned mic path, Whisper, Piper, push-to-talk and optional wake. Use invented fixtures and recorded speech tests for routing, silence, cancellation, playback and restarts. | In progress: local commands/lists/alarm delivery and opt-in push-to-talk adapters pass simulated tests. Wake detection, live spoken checks, resident Piper and dependency provenance remain open. No Pi acceptance. |
| **2. Owner integrations, in parallel** | Track A: package Home Assistant Container, owner authorization, selected-entity allowlist and synthetic household tests. Track B: isolate owner Codex CLI login/answer process, test with fake CLI and disposable signed-in environment, and handle auth/limit/network failure. Both connect to the Stage 1 router; neither blocks the other's implementation. | Planned. HA auth and Codex suitability remain separate release gates. |
| **3. Guided install and software acceptance** | Build an idempotent installer, setup wizard, selected mic/speaker checks, per-engine diagnostics, redacted support bundle, backup/update/rollback/uninstall. Require known-good spoken direct-worker and full app smoke tests. Run clean disposable Linux/HA installs, simulated failures and an outside-household setup trial before hardware. | Planned. Software acceptance cannot establish Pi performance. |
| **4. Dedicated Pi acceptance** | On a separate allowlisted Pi, test the complete HA/voice/Codex/kiosk stack, ARM64 dependencies, memory/latency/thermals, mic/speaker/display, normal-volume wake, silence/background/near-match and long speech, reboot, network loss, auth expiry, updates and rollback. Revise and repeat until a supported profile and independent fresh-owner install pass. | Waiting for dedicated hardware; final technical gate. The private pilot does not replace it. |
| **5. Public beta** | Publish the verified hardware/OS profile, limitations, installation and recovery guide, audited release archive and license/asset notices. Label the release beta until fresh-owner results support a stronger promise. | Blocked on stages 0–4. |

Stages 1 and 2 overlap after their shared contracts are fixed; installer and wizard work can start during Stage 2, with Stage 3 closing their integration and recovery gates. This shortens the critical path without dropping acceptance. Stage 4 stays after the software work and is the last technical gate before beta. Record any question needing physical evidence there; desktop tests do not prove Pi performance.

## Agent assignments and review

GPT-6 Luna Medium can implement fictional fixtures, UI, docs, CI, tests and narrow public modules under agreed contracts. Stage 1 core/voice and Stage 2 HA/Codex can have separate agents only in an isolated Alltron-only OS/container environment without private mounts or credentials. Same-host worktrees do not provide that isolation; on the current host, Luna is restricted to non-sensitive public tasks. GPT-6 Sol High reviews the reuse map and contracts, security-sensitive boundaries, integrated diffs, dependency provenance and exact public push range. The lead integrates and records release evidence.

## Privacy gate before every GitHub push

1. Start from the public Alltron history. Never import the private project's history, source tree, symlink, state, recordings, photos, models, credentials or deployment targets. Review an explicit path allowlist.
2. Run [the publication gate](PUBLICATION_GATE.md). Inspect **all new commits and Git objects**, including deleted files, binary assets and author/committer metadata. Run a secret scanner and manually check for names, addresses, device IDs, calendar data, media and personal configuration; `.gitignore` alone is insufficient.
3. If any questionable content entered a commit, rebuild from the clean public base before pushing. Removing it in a later commit does not remove exposure from public history.
4. Sol High reviews the exact SHA and final diff before a branch push. Use GitHub protection and scanning as additional controls, and verify release archives separately.
5. Keep every fixture invented and every support bundle redacted. Never publish account tokens, Codex auth state, real household data or current-Pi addresses. Source/model licensing and provenance review remains required even when using the same upstream engines as the private app.

The initial public GitHub commit predates this plan and contains a personal email in its Git metadata. The published development commits use the chosen GitHub no-reply identity. Repository history already public cannot be made private by a later commit.
